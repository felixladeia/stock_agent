[project]
name = "stock-agent-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
]

[tool.uv]
dev-dependencies = ["pytest>=8.0", "httpx>=0.27"]
